spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bryanarriaza/microservices-architecture/tree/master/config
server:
  port: 8888

